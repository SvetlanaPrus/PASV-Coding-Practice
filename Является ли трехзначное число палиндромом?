//Является ли трехзначное число палиндромом?

//Палиндром - число, которое читается одинаково слева направо и справа налево.
//Например, трехзначные числа 121, 464 - палиндромы. Числа 123, 489 - не палиндромы.
//Напишите функцию с именем isPalindrome, которая принимает трехзначное положительное число num в качестве аргумента и возвращает:
//"palindrome", если число является палиндромом, "not a palindrome", если число не является палиндромом.

//Примеры:

//функция isPalindrome(141) должна возвратить "palindrome";
//функция isPalindrome(111) должна возвратить "palindrome";
//функция isPalindrome(481) должна возвратить "not a palindrome".


function isPalindrome(num){
  let a = Math.floor(num / 100);
  let b = Math.floor((num % 100) / 10);
  let c = num % 10;
  if (c === a) return "palindrome";
  return "not a palindrome";
}
