Арифметические операции

Напишите функцию с именем calculate, которая принимает два числа a и b и строку operation (знак арифметической операции) 
в качестве аргументов, выполняет данную операцию над числами и возвращает результат в виде строки, содержащей решение. 
Возможные знаки операций: '+', '-', '*', '/', '%'. 
Результат деления чисел округлить до ближайшего целого числа с помощью метода Math.round(). 
Гарантируется, что во всех тестах будут использоваться только данные арифметические операции. В решении необходимо использовать оператор switch.

Примеры:

функция calculate(5, 3, '+') должна возвратить "5 + 3 = 8";
функция calculate(5, 3, '-') должна возвратить "5 - 3 = 2";
функция calculate(5, 3, '*') должна возвратить "5 * 3 = 15";
функция calculate(5, 3, '/') должна возвратить "5 / 3 = 2"; //результат округления
функция calculate(5, 3, '%') должна возвратить "5 % 3 = 2".


function calculate(a, b, operation){
switch (operation){
  case '+': return `${a} + ${b} = ${a + b}`;
  case '-': return `${a} - ${b} = ${a - b}`;
  case '*': return `${a} * ${b} = ${a * b}`;
  case '/': return `${a} / ${b} = ${Math.round(a / b)}`;
  case '%': return `${a} % ${b} = ${a % b}`;
}
}
