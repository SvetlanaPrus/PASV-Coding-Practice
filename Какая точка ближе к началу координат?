//Какая точка ближе к началу координат?

//Даны координаты двух точек (x1, y1) и (x2, y2) в прямоугольной системе координат. Определить, какая из точек находится ближе к началу координат.
//Напишите функцию с именем distanceComparison, которая принимает координаты точек (x1, y1) и (x2, y2) в качестве аргументов и возвращает:
//1, если точка (x1, y1) ближе к началу координат;
//2, если точка (x2, y2) ближе к началу координат;
//0, если точки находятся на одинаковом расстоянии от начала координат.

//Примеры:

//функция distanceComparison(1, 1, 2, 2) должна возвратить 1;
//функция distanceComparison(3, 4, 3, 0) должна возвратить 2;
//функция distanceComparison(3, 3, -3, 3) должна возвратить 0.


function distanceComparison(x1, y1, x2, y2){
  let d1 = (x1 ** 2 + y1 ** 2) ** 0.5;
  let d2 = (x2 ** 2 + y2 ** 2) ** 0.5;
    if (d1 < d2) return 1;
    if (d2 < d1) return 2;
    if (d1 === d2) return 0;
}
