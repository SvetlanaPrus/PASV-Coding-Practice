Какое количество кабинетов необходимо офису, чтобы разместить сотрудников?

В каждом кабинете офиса k рабочих мест. Какого наименьшего количества кабинетов достаточно, чтобы разместить number сотрудников?
Напишите функцию с именем officeRooms, которая принимает числа k (количество мест в одном кабинете) и number (количество сотрудников офиса) 
в качестве аргументов и возвращает наименьшее количество кабинетов, необходимое для размещения number сотрудников.

Примеры:
функция officeRooms(300, 5000) должна возвратить 17;
функция officeRooms(250, 5150) должна возвратить 21.


***********************************

function officeRooms(k, number){
  for (let i = 1; i <= number; i++){
    if (k * i >= number) return i;
  }
}
