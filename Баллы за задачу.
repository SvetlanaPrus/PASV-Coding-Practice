Баллы за задачу.

Сайт содержит набор задач по программированию.
Задачи имеют 3 уровня сложности: "easy", "medium", "hard".
За решение задачи уровня "easy" начисляется 2 балла, за решение задачи уровня "medium" начисляется 4 балла, за решение задачи уровня "hard" начисляется 8 баллов.
Напишите функцию scoreIncrease, которая принимает две величины current (текущее количество баллов) и 
level (уровень сложности задачи) в качестве аргументов и возвращает новое количество баллов, полученное после решения задачи. 
В решении необходимо использовать тернарный оператор.

Примеры:

функция scoreIncrease(400, 'easy') должна возвратить 402;
функция scoreIncrease(200, 'medium') должна возвратить 204;
функция scoreIncrease(302, 'hard') должна возвратить 310.


function scoreIncrease(current, level){
  if (level === 'easy'){
    return current + 2;
  } else return (level === 'medium')? current + 4 : current + 8;
}
