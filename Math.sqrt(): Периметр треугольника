Периметр треугольника

Даны координаты трех точек плоскости - вершин треугольника: A(x1, y1), B(x2, y2), C(x3, y3). Расстояния между точками вычисляются по формулам: ...
Напишите функцию с именем perimeter, которая принимает шесть чисел (три пары чисел - координаты вершин треугольника) x1, y1, x2, y2, x3, y3 в качестве аргументов 
и возвращает периметр треугольника. Результат округлите до двух десятичных знаков.

Примеры:
функция perimeter(1, 1, 4, 1, 1, 5) должна возвратить 12.



******************************

function perimeter(x1, y1, x2, y2, x3, y3){
  let ab = Math.sqrt( Math.pow(x2 - x1, 2) + Math.pow(y2 - y1, 2) );
  let ac = Math.sqrt( Math.pow(x3 - x1, 2) + Math.pow(y3 - y1, 2) );
  let bc = Math.sqrt( Math.pow(x3 - x2, 2) + Math.pow(y3 - y2, 2) );
    return +(ab + bc + ac).toFixed(2);
}
