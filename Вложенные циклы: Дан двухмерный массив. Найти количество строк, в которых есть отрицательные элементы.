Дан двухмерный массив. Найти количество строк, в которых есть отрицательные элементы.

Напишите функцию с именем countLinesWithNegativeElements, которая принимает двухмерный массив arr в качестве аргумента 
и возвращает количество строк, содержащих хотя бы один отрицательный элемент.

Примеры:
функция countLinesWithNegativeElements([[1, 2], [2, -2, -3], [3, 5], [3, 4, 5, -1, 8]]) должна возвратить 2; 
(так как только две строки (два элемента массива) имеют отрицательные элементы: [2, -2, -3], [3, 4, 5, -1, 8].

функция countLinesWithNegativeElements([[1, -2], [2, -2, -3], [3, 5], [3, 4, 5, -1, 8], [-3, -1]]) должна возвратить 4; 
(так как четыре элемента массива имеют отрицательные элементы: [1, -2], [2, -2, -3], [3, 4, 5, -1, 8], [-3, -1].

функция countLinesWithNegativeElements([[1, 3], [2, 7, 8], [3, 5]]) должна возвратить 0; 
(так как нет ни одного элемента массива с отрицательными элементами.)



function countLinesWithNegativeElements(arr){
let count = 0;
  for (let i = 0; i < arr.length; i++){
      for (let j = 0; j < arr[i].length; j++){
        if (arr[i][j] < 0) {count = count + 1; break;}
      }
  }
  return count;
}

