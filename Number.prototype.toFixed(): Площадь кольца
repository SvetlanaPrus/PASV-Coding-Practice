Площадь кольца

Напишите функцию ringSquare, которая принимает 2 числа r1, r2 (внешний и внутренний радиусы кольца) (r1 > r2) 
в качестве аргументов и возвращает площадь кольца. Результат округлите до 2 знаков после запятой и возвратите в виде числа.

Примеры:

функция ringSquare(2, 1) должна возвратить 9.42.
функция ringSquare(5, 4) должна возвратить 28.27.


**********************************************

function ringSquare(r1, r2){
  let s = Math.PI * Math.pow(r1, 2) - Math.PI * Math.pow(r2, 2);
  return +(s).toFixed(2);
}
