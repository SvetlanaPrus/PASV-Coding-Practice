Присвойте переменным новые значения

Даны две переменные a, b, содержащие числа.
Если они не равны, присвойте каждой переменной значение большего из этих чисел, если переменные равны, присвойте каждой переменной значение 0.
Напишите функцию с именем newValues, которая принимает два числа a, b в качестве аргументов и возвращает новые значения переменных a, b в виде массива [newA, newB].

Примеры:

функция newValues(5, 1) должна возвратить [5, 5];
функция newValues(2, 9) должна возвратить [9, 9];
функция newValues(3, 3) должна возвратить [0, 0].


function newValues(a, b){
  if (a === b) return [0, 0];
  return (a > b)? [a, a] : [b, b];
}
