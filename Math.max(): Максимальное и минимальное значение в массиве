Максимальное и минимальное значение в массиве

Напишите функцию maxMin, которая принимает массив arr в качестве аргументов и возвращает массив, 
содержащий максимальное и минимальное значение: [max, min]. Если массив пустой, вернуть [].

Пример:

функция maxMin([3, 4, 8, 1]) должна возвратить [8, 1];
функция maxMin([7, 7]) должна возвратить [7, 7];
функция maxMin([2]) должна возвратить [2, 2];
функция maxMin([]) должна возвратить [];


********************************


function maxMin(arr){
  if (arr.length === 0) return [];
  return [Math.max(...arr), Math.min(...arr)];
}
