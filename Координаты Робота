Координаты Робота

Известны начальные координаты Робота (x, y). Робот может двигаться в четырех направлениях: N - север, S - юг, W - запад, E - восток. 
При перемещении в определенном направлении из точки (x, y) соответствующая координата Робота изменяется на 1:
при перемещении на север Робот оказывается в точке (x, y+1),
при перемещении на юг Робот оказывается в точке (x, y-1),
при перемещении на запад Робот оказывается в точке (x-1, y),
при перемещении на восток Робот оказывается в точке (x+1 y).

Напишите функцию с именем robot, которая принимает три аргумента: числа x, y (начальные координаты Робота) 
и строку direction, содержащую направление движения ('N', 'S', 'E', 'W') и возвращает новые координаты Робота в виде массива x, y.
В решении необходимо использовать оператор switch.

Примеры:
функция robot(5, 5, 'E') должна возвратить [6, 5];
функция robot(5, 5, 'W') должна возвратить [4, 5];
функция robot(5, 5, 'N') должна возвратить [5, 6];
функция robot(5, 5, 'S') должна возвратить [5, 4].


function robot(x, y, direction){
switch (direction){
  case 'E': return [x + 1, y];
  case 'W': return [x - 1, y];
  case 'N': return [x, y + 1];
  case 'S': return [x, y - 1];
}
}
